import { styled } from "@mui/system";
import { Stack } from "@mui/material";

const RowStart = styled(Stack)({
  display: "flex",
  flexDirection: "row",
  justifyContent: "flex-start",
  alignItems: "flex-start",
});

export default RowStart;
