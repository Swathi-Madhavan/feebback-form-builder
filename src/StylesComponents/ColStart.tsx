import { styled } from "@mui/system";
import { Stack } from "@mui/material";

const ColStart = styled(Stack)({
  display: "flex",
  flexDirection: "column",
  justifyContent: "flex-start",
  alignItems: "flex-start",
});

export default ColStart;
